[project]
name = "web-threat-intelligence"
version = "1.0.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "pandas",
    "tldextract",
    "scikit-learn",
    "shap",
    "xgboost"
]

[tool.vercel]
entrypoint = "backend.app:app"